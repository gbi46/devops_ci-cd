controller:
  adminUser: admin
  adminPassword: admin123
  serviceType: ClusterIP
  resources:
    requests:
      cpu: "200m"
      memory: "512Mi"
    limits:
      cpu: "1"
      memory: "1Gi"
  persistence:
    enabled: false